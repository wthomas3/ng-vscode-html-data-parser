/**
 * A reference for an attribute shown in completion and hover.
 */
export interface Reference {
  /**
   * The name of the reference.
   */
  name: string;
  /**
   * The URL of the reference.
   */
  url: string;
}
